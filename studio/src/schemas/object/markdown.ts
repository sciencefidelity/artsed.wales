import { FaMarkdown } from 'react-icons/fa'

export default {
  name: 'markdown',
  type: 'object',
  title: 'Markdown',
  icon: FaMarkdown,
  fields: [
    {
      name: 'markdown',
      type: 'text',
      title: 'Markdown'
    }
  ]
}
